需求：前端用react撰写一个页面，用于展示excel表格，用ts撰写代码
- 用pnpm包管理器
- 前端页面需要有一导入的按钮，选择excel文件，后端解析表头字段，解析后，可以选择需要在页面中展示的列
- 表格中必定存在是否合格，质检员业务反馈意见的列，这几列必定需要展示，而且需要编辑，其他所有列都只用展示，其中的列可能会有图片，需要对图片也能提取展示
- 需要支持多文件切换，左侧给一个导航栏，支持不同的excel文件
- 如果需要数据库那么使用sqlite3
- 表格中必定有level1，level2，这两列的值可以筛选